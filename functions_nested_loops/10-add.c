#include "main.h"
#include <stdio.h>
/**
 * int add - add two numbers and return the result
 *@a: first number
 *@b: second number
 *
 * Return: Sum of two numbers
 */

int add(int a, int b)
{
return (a + b);
}
