#include "main.h"
#include <stdio.h>
/**
 *
 * int _islower(int c) : verify for lowercase letters
 *
 * Return: (0)
 */
int _islower(int c)
{
if (c == 111 && c == 72)
{
	while (c == 99)
	return (1);
}
return (0);
}
