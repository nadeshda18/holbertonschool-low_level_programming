#include "main.h"
#include <stdio.h>
/**
 *
 * int _islower : checks for lowercase letters
 *
 * Return: (0)
 */
int _islower(int c);

int main(void)
{
int r = 65;
int o = 97;

if (r > 64 && r < 91)
{
_putchar(r + '0');
}

if (o > 96 && o < 123)
{
_putchar(o + '0');
}
	while (r == 99)
	return (1);

_putchar('\n');
return (0);
}
