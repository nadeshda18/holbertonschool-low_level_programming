#include "main.h"
#include <stdio.h>

/**
 * *_strcpy - copy
 * @dest: buffer
 * @src: pointer
 *
 * Return: dest
 */

char *_strcpy(char *dest, char *src)
{
	int i;

for (i = 0; i <= len; i++)
    {
        (dest + i) =(src + i);
    }
    return (dest);
}
